# Master List of Tools — Load Folder

---

## Programming Languages
- **Python**
- **SQL** (T-SQL / Microsoft SQL Server syntax)
- **SAS**

---

## Python Libraries & Packages

### Data Manipulation & Analysis
- `pandas`
- `numpy`
- `scipy` (stats)
- `csv`, `json`, `glob`, `re`, `os`, `sys`

### Machine Learning
- `scikit-learn` — LinearRegression, RandomForestRegressor, GradientBoostingRegressor, KMeans, train_test_split, LabelEncoder, MinMaxScaler, StandardScaler, roc_auc_score, MSE, R²

### Data Visualization
- `matplotlib`
- `seaborn`
- `plotly` (express, graph_objs)

### Web / API
- `requests`, `urllib`
- `FastAPI` (with uvicorn, CORSMiddleware, HTMLResponse, StaticFiles)
- `pydantic`

### File Handling
- `openpyxl` — Excel read/write
- `python-docx` (`docx`) — Word documents
- `python-pptx` (`pptx`) — PowerPoint
- `PyPDF2` — PDF reading

### Web Scraping
- `BeautifulSoup` (`bs4`)
- `pytube`

### AI / NLP / Search
- `openai`
- `faiss` — vector similarity search
- `sentence-transformers` (`SentenceTransformer`)

### Database Connectivity
- `pyodbc` — ODBC (SQL Server, Access)

### SAS Integration
- `saspy`
- `sas7bdat`

### Utilities
- `fuzzywuzzy` — fuzzy string matching
- `unidecode` — text normalization
- `tkinter` — GUI
- `turtle` — graphics
- `pickle` — object serialization
- `ipywidgets` — Jupyter widgets
- `datetime`, `time`, `random`, `typing`

---

## SAS Procedures (PROC)
- `PROC PRINT`, `PROC MEANS`, `PROC FREQ`, `PROC CONTENTS`, `PROC SQL`, `PROC IMPORT`, `PROC UNIVARIATE`, `PROC STDIZE`

---

## Databases & Data Platforms
- **Microsoft SQL Server** (T-SQL, BULK INSERT, `USE [db]`)
- **Microsoft Access** (via pyodbc)
- **DB2 / Mainframe** (referenced in project descriptions)
- **SAS Studio** (OnDemand for Academics — `/home/u63685170/`)

---

## Applications & Software Tools
- **Tableau** / `.twbx` workbooks
- **Microsoft Excel** (`.xlsx`, `.xlsm`, `.xlsb`)
- **Microsoft Word** (`.docx`)
- **Microsoft PowerPoint** (`.pptx`)
- **Power BI** (referenced in project descriptions)
- **ArcGIS** / GIS (`.shp`, `.dbf`, `.prj`, shapefile formats)
- **Jupyter Notebooks** (`.ipynb`)
- **VS Code**
- **SAS** (Studio / OnDemand)

---

## Platforms & Services
- **GitHub** / GitHub Pages
- **OpenAI API**
- **NHL API** (public hockey stats API)
- **DuckDNS** (dynamic DNS — `hbr-bida.duckdns.org`)
- **Azure** (referenced)
- **Google** (folder: "All 2 Google")

---

## File & Data Formats
- `.csv`, `.json`, `.xlsx`, `.xlsm`, `.xlsb`, `.txt`, `.pdf`, `.docx`, `.pptx`, `.twbx`, `.sas`, `.sasnb`, `.shp` (shapefile)
